#include <stdio.h>
#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: alphabet to check
 * Return: 1 if c is uppercase
 * 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
