#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: integer to be computed
 * Return: n if the integer is absolute
 * -n otherwise
 */
int _abs(int i)
{

	if (i >= 0)
	{
		return (n);
	}
	return (-n);
}
